# CLAUDE.md

## このプロジェクト

「何者か」— ビッグファイブ性格診断PWA。35問すべて二択。バニラHTML/CSS/JS、ビルド不要。`src/` を静的配信するだけで動く。
（旧称「気質図」→「ワッサー診断」。localStorage のキーは `nanimonoda.v4` / `nanimonoda.theme`。**旧版はタグに保存済み** — `v1-2axis`（2軸12問）/ `v2-3axis`（3軸18問・4段階評定）。`git checkout <tag>` で戻れる）

- 尺度は**ビッグファイブ**（外向性・協調性・誠実性・情緒安定性・開放性）。**5因子 × 7問 = 35問、すべて二択（強制選択）**
- **二択にしている理由が3つある。** ①「そう/違う」の軸が無いので黙従バイアスが原理的に起きない ②1因子7問＝奇数なので中央値に到達できない ③合計がそのまま4段階に割れるので補正計算が要らない
- **1因子を偶数問にしないこと。** 中央値が出るようになる
- **出力はビッグファイブそのもの。** 名付け（`archName`）→ 辛口の一言（`quipLine`）→ 正五角形のレーダー → 陽陰×温冷の座標盤 → 因子ごとの読み（`READ`）
- **数値は10段階（`ten()`）で、分母を出さない。** 「2/10」だと劣って見えるので「協調性 2」と置く
- **設問画面に因子名を出さない。** 何を測られているか意識すると答えが歪む
- **結果の言葉は「ちょっと辛口 → 必ず最後は認める」で書く。** `QUIP` と `READ` の全項目がこの型。低い側を欠点として言い切らないこと
- **口調は言い切りで統一する。** デスマス調と混ぜない。辛口とエンタメ性に合うのは言い切りのほうで、混在すると診断の芯がぼやける。UI文言も含めて全部
- **名付け（`ARCHETYPE` / `PREFIX`）は硬い役職名にしない。** 覚えて人に言いたくなる名前にする（宴の主役・段取りの鬼・好奇心モンスター など）
- **役職適性（代表・副代表など）の算出は廃止した。** 診断結果は性格そのものを返し、役職に当てはめる解釈は人がやる
- 他己評価は**URLのフラグメント（`#`以降）で受け渡す。** 35問を5バイト（1問1ビット）に詰めている。上限は8件
- 保存: localStorage のみ。サーバー送信は一切しない

## 最初に読むもの

1. `docs/01-測定設計.md` — 軸の定義、設問の設計原則、採点仕様、全設問リスト
2. `docs/02-決定ログ.md` — なぜ今の形なのか。却下した案とその理由
3. `docs/03-次のタスク.md` — 今やること

**`docs/` は公開リポジトリに含めていない**（`.gitignore` 済み）。サークルの代表・副代表の選抜方針など内部の判断が書かれているため、手元にだけ置いている。クローンしてきた環境には存在しないので、その場合は開発者に渡してもらうこと。

**`docs/01-測定設計.md` を読まずに設問を触らないこと。** 見た目は普通の文章だが、1問1問に測定上の制約がかかっている。

## 絶対に守るルール

- **1因子7問を維持する。** 偶数にすると中央値が出る
- **低い側の呼び方を否定的にしない**（率直 / おおらか / 感じやすい / 現実的）。欠点に見えると全員が高い側に寄る
- **`READ`（因子ごとの読み）は、低い側にも必ず利点を書く。** どの因子も高い側が良いという意味ではない
- **`hi`（高い側の選択肢番号）を0と1に散らす。** 片側に寄せると「同じ側を押し続ける」癖が出て、二択にした意味が消える
- **1問1側面。** 「返答やリアクションや意思表示をする」のように複数の行動を1問に入れない
- **設問を差し替えるときは `facet` を変えない。** 言い回しの変更はOK、測る側面の変更はNG
- **50%ちょうどは陰側・冷側・緩側に含める**（`band()` 参照）
- **設問に役職名を出さない。** 「代表になりたいか」で答えられてしまう。設問は性格を聞き、役職は `ROLES` の重みから導く
- **自己評価と他己評価の facet と hi を揃えたままにする。** ここがずれた瞬間、版どうしの差分が意味を失う
- **共有リンクの形式(`readLink`/`makeLink`)を変えるときは `LINK_V` を上げる。** 古いリンクを新しい解釈で読むと、他人の診断結果が黙って別物になる
- **他己評価の注記(`PEER_NOTE`)を消さない。** 外から見えない側面（一人時間の選好・社交後の消耗・情動伝染）が推測で埋められていることを、必ず読み手に伝える

## コード構成

- `src/index.html` — 画面骨格とスタイル。CSS変数はすべて `:root` に集約
- `src/app.js` — 設問データ(`QS`)、タイプ定義(`TYPES`)、配色制御(`theme`)、採点(`score`/`band`)、共有リンク(`makeLink`/`readLink`)、座標盤描画(`field`/`dot`/`drawSingle`/`drawCompare`)、画面制御
- 役職適性は `ROLES` の重み × 3軸で算出(`fitOf`/`roleFits`)。副代表は2枠を高いほうに畳んで1つだけ表示する
- **重みを変えたら役職どうしの近さ（重みベクトルのcos）を測ること。** 0.85を超えると実質同じ役職になり、得点が団子になる
- 画面は6つ: 表紙 / 設問 / 結果 / 他人の診断を送る / 受け取った他己評価 / 並べて見る
- 外部依存ゼロ。フレームワーク・ビルドツールを導入しないこと

## デザイン方針

**ニューモーフィズム。** 背景と同じ色の面が、影だけで浮き沈みして見える表現。

- **光源は常に左上。** 明るい影(`--hl`)を左上、暗い影(`--sd`)を右下に置く。1箇所でも逆にすると全体の立体感が壊れる
- **面と要素は同色。** 要素に別の背景色を敷かない。区切りに罫線・枠線を使わない（影が境界の役割を持つ）
- **状態は凹凸で表す。** 押せるもの＝浮き(`--out`)、押した/選ばれた/無効＝窪み(`--in`)、値が入る器＝窪み
- 影のトークンは `:root` の `--out` / `--out-sm` / `--out-lg` / `--in` / `--in-deep` に集約。個別に box-shadow を手書きしない
- **低コントラストになりやすいのが弱点。** 本文は `--ink` を使い、影の淡さに合わせて文字まで薄くしないこと

### 配色は1つだけ

ミッドナイトブルー `#0D1428` の1本。宇宙。ごく淡い星と遠い光を `body::before` に置いている。ミニマルを保つため星は小さく数を絞る。

**ライト配色は廃止した。切り替えも持たない。** 影の淡さで面を作る表現はもともと暗い地のほうが素直に出るうえ、光として置いた色が白地では濃色になって影に見える、という事故がライト側だけで起き続けた。`prefers-color-scheme` にも `data-theme` にも反応しない。
**色は必ず CSS変数を経由する。** SVGの中も含めて生の色値を書かないこと。書いた瞬間、片方の配色で破綻する。

### 語彙と書体

- **気象のメタファーと語彙は使わない。** 気象図・前線・気圧・日照・日陰・暖気・寒気などは全廃した。2軸の座標として素直に呼ぶ
- **書体はヒラギノ角ゴシック1系統のみ。** W3(`font-weight:300`)を本文、W6(`600`)を見出しと強調に。明朝・モノスペースは使わない
- 主役は正五角形のレーダー(`radarChart`)。**線は紫**（`--rd-web` / `--rd-num`。色面用の `--grid` は薄すぎて沈むので使わない）。頂点に因子名と10段階の数値を出す
- 座標盤は補助。横=外向性を**陽陰**、縦=協調性を**温冷**と表示する（利用者にはこの呼び名で通す）
- 二択は左右に並べた縦長の角丸長方形。**周囲をパープルの光が回る**（`.opt::before` の円錐グラデーションを回転。光が当たらない区間も枠が見えるよう、下に一様な紫を敷いてある）診断の色: 自己評価 `--cat-a`（ティール）/ 他己評価 `--cat-c`（ローズ）

### 座標盤の向き

**外向=右 / 内向=左 / 協調=上 / 率直=下。****座標盤は2軸のみ。点は1点だけで輪を付けない。**地は四隅の色を直接指定した双一次の面**（`--c-tl`/`--c-tr`/`--c-bl`/`--c-br`、濃さは `--wash`）。半透明を4辺から重ねる方式は中央が抜けて彩度が出ないので使わない。**イラストもアニメーションも置かない。** 点は小さな光点（`.fx-dot`）で、自己評価=パープル（芯・にじみとも紫）、他己評価=ホワイト。**大きさは同じにして色だけで分ける。発光色に配色変数を使わないこと**（ライト配色で濃色になり影として沈む）。**点の脇に名前は出さない** — 盤の中の文字は軸ラベル4つだけ、識別は色と凡例で — ライト配色で濃色になり影として沈む — 芯まで色を付けないこと、色を全部消さないこと（比較画面で自己と他己が見分けられなくなる）。目盛りと軸ラベルは白系。プロットは `PLOT`（28〜272）に収める。四辺の28は軸ラベルの居場所で、これ以上は詰められない。係適軸は載せない。** 一度リング表示と立体表示を実装したがどちらも廃止した（理由は `docs/01-測定設計.md` 7.5節）。係適は数値タイルと役職適性の六角形・バーで見せる。`px()` が外向性、`py()` が協調性を受け取る点に注意（引数を取り違えても動いてしまい、プロットだけが静かに狂う）。

### アイコン

4色の弧が1つの環をつくるマーク。**軸の色とは無関係の独立した4色**で、ひとりの中にいくつもの面があることを表している。生成スクリプトはリポジトリに置いていないので、作り直すときはこの意図から起こすこと。

### モバイル

**スマホで開かれる前提。** 320px幅で全画面が横スクロールなしに収まることを維持する。タップ対象は最小44px、入力欄の文字は16px以上（下回るとiOSが勝手に拡大する）。

**この世界観を崩す変更をするときは必ず確認を取ること。**

## 動作確認

```bash
cd src && python3 -m http.server 8000
```

Service Worker は https か localhost でのみ動く。

**開発中に変更が反映されないときは、まずキャッシュを疑う。** `sw.js` は cache-first なので、`CACHE` の版を上げずにファイルだけ差し替えても古い版が出続ける。ブラウザのコンソールで下記を実行してから再読み込みする。

```js
(await navigator.serviceWorker.getRegistrations()).forEach(r => r.unregister());
(await caches.keys()).forEach(k => caches.delete(k));
```
